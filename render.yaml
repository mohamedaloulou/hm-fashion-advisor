services:
  - type: web
    name: hm-fashion-advisor
    runtime: python
    pythonVersion: "3.11.9"
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app2.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PINECONE_API_KEY
        sync: false